/*The function below is part of the extra credit activity. The quoteChnager getRandomQuotefunction
uses the setInterval method to automatically refresh the browser every 10 seconds */
function quoteChanger(){
  setInterval(printQuote, 10000);
}

quoteChanger();
